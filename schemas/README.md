# Schemas

Formal JSON Schema files can be added once the YAML structure stabilizes.

Current top-level data structures:

- `components.yaml` -> `components: []`
- `flows.yaml` -> `flows: []`
- `views.yaml` -> `views: []`
- `threats.yaml` -> `threats: []`
- `controls.yaml` -> `controls: []`
